#Note: Self defined approach to ignore the line commented with sharp/hash symbol at the beginning.
#      Not common standards for comments on json.
{	
	"scheduler": {
		"bootShell": "/data/scheduler/bin/run.sh",
		"jobShells": "/data/scheduler/shells",
		"jobJars": "/data/scheduler/jobs",
		"daemonFrequency": 300,
		
		# db settings option
		"db_settings": {
		    "dbs": [{
		    	# db id.
		    	"id": "scheduler",
		    	
		    	# specified host name.
		        "host": "localhost",
		        
		        # db port.
		        "port": 3306,
		        
		        # specified username.
			    "username": "root",
			    
			    # specified password.
			    "password": "angelfish",
			    
			    # specified db name.
		        "name": "scheduler"
		    }]
		}
    }
}